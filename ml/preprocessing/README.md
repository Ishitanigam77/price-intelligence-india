# ml/preprocessing/

Train-only categorical encoding and numeric matrix assembly. Unknown categories and missing
numerics are `NaN`, never target-filled.

**Status**: implemented in **Phase 10 — Sale-Price Prediction**.
