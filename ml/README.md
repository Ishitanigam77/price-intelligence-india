# ml/

Machine learning pipelines: feature engineering, XGBoost sale-price prediction model training,
and inference serving consumed by the Recommendation Engine.

**Status**: empty scaffold. Introduced in **Phase 8 — ML: Sale-Price Prediction**. No models,
trained artifacts, or training code exist here yet.

## Layout

- `training/` — offline training pipelines
- `inference/` — prediction serving
- `features/` — feature engineering shared by training and inference
- `notebooks/` — exploratory analysis (not used for production logic)
