"""Unit tests for observability primitives, telemetry export, and request middleware.

Phase 1 coverage of `app.domain.validation` lives in `test_domain_validation.py`.
Adapter-framework coverage lives in `retailer_adapters/`.
"""
