# app/auth/

Clerk integration: verifies Clerk-issued tokens on protected backend routes and exposes the
authenticated user's identity to the API layer. No custom password/session handling.

**Status**: empty scaffold. Introduced in **Phase 5 — Search, Compare & Frontend MVP**.
