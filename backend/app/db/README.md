# app/db/

SQLAlchemy engine/session setup and declarative base. Actual table models live here once
introduced; migrations are managed separately under `../../alembic/`.

**Status**: empty scaffold. Introduced in **Phase 1 — Core Domain Model & Database
Foundation**.
