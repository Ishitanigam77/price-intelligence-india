"""SQLAlchemy engine/session setup, declarative base, and ORM models.

Table models live under `app.db.models`. Migrations are managed separately under
`backend/alembic/` and must be kept in sync with these models.
"""
