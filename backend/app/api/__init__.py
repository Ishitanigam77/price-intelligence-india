"""FastAPI routers and request/response DTOs (HTTP concerns only).

Business logic lives in `app.domain`, `app.repositories`, and (starting later phases)
`app.pricing`, `app.matching`, etc. — never here.
"""
