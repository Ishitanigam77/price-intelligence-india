# pipelines/

Azure DevOps pipeline (YAML) definitions: CI (build, lint, test, security scanning) and CD
(build image → push to ACR → deploy dev → integration tests → manual approval → deploy
production).

**Status**: empty scaffold. Introduced in **Phase 11 — Infrastructure & Production
Readiness**.
