# terraform/modules/

Reusable Terraform modules (one per Azure resource category, e.g. networking, database, cache,
container registry, key vault, monitoring).

**Status**: empty scaffold. Introduced in **Phase 11 — Infrastructure & Production
Readiness**.
